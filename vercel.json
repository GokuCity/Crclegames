{
  "buildCommand": "npm install --prefix shared && npm run build --prefix shared && npm install --prefix client && cd client && tsc && vite build",
  "outputDirectory": "client/dist",
  "framework": null,
  "installCommand": "npm install --legacy-peer-deps"
}
